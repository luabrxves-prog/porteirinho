<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!-- Shell-only harness. Never packaged into the normal debug/release APK. -->
        <receiver android:name="com.rondasafe.app.E2eSyncReceiver" android:exported="true" android:permission="android.permission.DUMP" />
    </application>
</manifest>
